import React from 'react'
import ReactDOM from 'react-dom/client'
import WRManager from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WRManager />
  </React.StrictMode>
)
